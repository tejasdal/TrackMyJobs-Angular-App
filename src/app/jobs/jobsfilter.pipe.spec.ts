import { JobsfilterPipe } from './jobsfilter.pipe';

describe('JobsfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new JobsfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
